import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-challange-marcos-klein',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
